/**
 * PeerLink Paper Design System — Spacing Tokens
 * Consistent 4-point grid.
 */

export const Spacing = {
  /** 2px — hairline */
  hairline: 2,
  /** 4px — tiny */
  xxs: 4,
  /** 6px — extra small */
  xs: 6,
  /** 8px — small */
  sm: 8,
  /** 12px — medium-small */
  ms: 12,
  /** 16px — medium */
  md: 16,
  /** 20px — medium-large */
  ml: 20,
  /** 24px — large */
  lg: 24,
  /** 32px — extra large */
  xl: 32,
  /** 48px — 2xl */
  xxl: 48,
  /** 64px — 3xl */
  xxxl: 64,
} as const;

/** Standard screen horizontal padding */
export const ScreenPadding = 20;

/** Card internal padding */
export const CardPadding = 16;
